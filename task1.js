/*Реализуйте функцию getUserData, которая принимает идентификатор пользователя (ID) в качестве аргумента и использует 
fetch для получения данных о пользователе с заданным ID с удаленного сервера. Функция должна возвращать промис, 
который разрешается с данными о пользователе в виде объекта. Если пользователь с указанным ID не найден, 
промис должен быть отклонен с соответствующим сообщением об ошибке.

Подсказка, с последовательностью действий:
getUserData использует fetch для получения данных о пользователе с удаленного сервера. Если запрос успешен (с кодом 200), 
функция извлекает данные из ответа с помощью response.json() и возвращает объект с данными о пользователе. Если запрос неуспешен, 
функция отклоняет промис с сообщением об ошибке. */

/*function getUserData(userID) {
    // Возвращаем новый промис, который выполнится асинхронно
    return new Promise((resolve, reject) => {
        // Отправляем GET-запрос к серверу для получения данных о пользователе по указанному ID
        fetch(`https://jsonplaceholder.typicode.com/users/${userID}`)
            .then(response => {
                // Проверяем успешность запроса
                if (!response.ok) {
                    // Если запрос не успешен, отклоняем промис с сообщением об ошибке
                    throw new Error('User not found');
                }
                // Если запрос успешен, извлекаем данные о пользователе в формате JSON
                return response.json();
            })
            .then(userData => {
                // Разрешаем промис с данными о пользователе
                resolve(userData);
            })
            .catch(error => {
                // Если произошла ошибка при выполнении запроса, отклоняем промис с сообщением об ошибке
                reject(error);
            });
    });
}

// Пример использования функции
const userID = 1;
getUserData(userID)
    .then(userData => {
        console.log(userData); // Выводим данные о пользователе
    })
    .catch(error => {
        console.error(error); // Выводим сообщение об ошибке, если пользователь не найден
    });*/